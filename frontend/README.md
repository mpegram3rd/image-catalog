# Getting Started

This is a fairly basic `React` app that has been bootstrapped by [Vite](https://vite.dev/guide/) 

To get up and running you should simply do the following:
```terminaloutput
npm ci
npm run dev
```

This will start a server on port `5174`.  
To use the web app just go to http://localhost:7174

